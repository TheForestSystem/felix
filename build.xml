<?xml version="1.0" encoding="UTF-8"?>
<project name="MethodsAssignmentJava" default="default" basedir=".">
    <description>Builds, tests, and runs the project MethodsAssignmentJava.</description>
    <import file="nbproject/build-impl.xml"/>

    <!-- Define a target for Javadoc generation -->
    <target name="-javadoc-build" depends="init,compile">
        <mkdir dir="dist/javadoc"/>
        <javadoc source="11" destdir="dist/javadoc">
            <fileset dir="src"/>
            <!-- Add any additional options or includes/excludes here -->
        </javadoc>
    </target>
</project>
